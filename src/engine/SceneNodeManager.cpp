#include "ObjectManager.h"

namespace engine
{
    void ObjectManager::updateAllNodes()
    {
        // Make a stack
        // Iterate over all objects:
        // * ignore inactive objects
        // * push "root" nodes to stack
        // * process each node
        // ** nodes with children calls updateNode(i)
        return;
    }
}